[pytest]
# Test modules share basenames across packages (server/mcp/tests/test_main.py
# and server/ws_subscription/tests/test_main.py, etc.). Default 'prepend'
# import mode rejects identical basenames with "import file mismatch".
# importlib mode has no basename-uniqueness requirement.
addopts = --import-mode=importlib -q
