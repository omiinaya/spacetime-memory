[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
ignore = ["E501"]
select = ["E4", "E7", "E9", "F", "W"]

# Command modules register click groups at import time (side-effect imports).
[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
